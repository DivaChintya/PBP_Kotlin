<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Luas_BangunDatar"
        tools:targetApi="31">

        <!-- Launcher pertama adalah NamaActivity -->
        <activity
            android:name=".NamaActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Splash Activity setelah klik next -->
        <activity android:name=".SplashActivity" />

        <!-- Menu utama setelah splash -->
        <activity android:name=".menuActivity" />

        <!-- Layout untuk menghitung luas persegi -->
        <activity android:name=".persegiActivity" />

        <activity android:name=".lingkaranActivity" />

        <activity android:name=".segitigaActivity" />

    </application>

</manifest>
